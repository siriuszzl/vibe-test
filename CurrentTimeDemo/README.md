# CurrentTimeDemo

A minimal SwiftUI iOS demo app that displays the current date and time.

## Run

1. Open `CurrentTimeDemo.xcodeproj` in Xcode.
2. Select an iPhone simulator or a connected iPhone.
3. Press Run.

The time updates every second while the app is open.
